



//Write a loop that will iterate from 0 to 15. 
for (let i = 0; i<16 ; i++) {
    let half = (i / 2);  //For each iteration it will check if the current number is odd or even
    if (Number.isInteger(half)) {  // display a message in the console window. 
    console.log(`${i} is even.`);
    } else {
    console.log(`${i} is odd.`);
} 
}



